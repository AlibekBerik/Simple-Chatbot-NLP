# Simple-Chatbot-NLP
# Simple Chatbot using Machine Learning (NLP)  This is a **simple chatbot** built using **Python**, **NLTK**, and **scikit-learn**.   It uses **TF-IDF vectorization** and **cosine similarity** to find the best response to user input.
